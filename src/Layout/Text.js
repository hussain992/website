import styled from 'styled-components';
import {
  space,
  color,
  width,
  display,
  fontSize,
  fontWeight,
  textAlign,
  letterSpacing,
  lineHeight,
  fontFamily,
  borders,
  borderColor,
} from 'styled-system'

const Text = styled.div`
  ${space}
  ${color}
  ${width}
  ${display}
  ${fontSize}
  ${fontWeight}
  ${textAlign}
  ${letterSpacing}
  ${lineHeight}
  ${fontFamily}
  ${borders}
  ${borderColor}
`;

export default Text;